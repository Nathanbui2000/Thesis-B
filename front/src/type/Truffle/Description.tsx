// import { IUser } from "./user";

export interface Description {
    anitqueWidth: number;
    antiqueHeight: number;
    antiqueID: number;
    antiqueLength: number;
    descriptionID: number;
    fileName: string;
    fileSize: number;
    fileType: string;
    filehash: string;
    materialCreated: string;
    uploadTime: number;
    uploader: string;
}